import styled from "styled-components";

export const StyledFavourites = styled.section`
  position: absolute;
  bottom: -50px;
  left: 0;
  width: 80%;
  overflow: hidden;

  ul {
    display: flex;
    align-items: center;
  }
`;
